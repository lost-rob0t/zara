#+title: RAGE #246 — built-in todo surface recovery

* Iteration 1 — recover stale PR #248 on current master
:PROPERTIES:
:issue: 246
:rage_start: ed683b454bc15d14ca4fdec329640a1bf1d431d1
:source_pr: 248
:source_head: 6ceb97ac7018eabc43ae1d70110d71c9be518bb3
:END:

** Research / reconciliation
- Operator priority requires Zara's built-in todo replacement to remain cleanly disable-able while daemon timers remain separate.
- Existing PR #248 implements that bounded contract but is non-mergeable and was based on 2054ac1d368b175a85518c258f6a9cf6647f69a4.
- Current master at recovery start is ed683b454bc15d14ca4fdec329640a1bf1d431d1.
- PR #248 changes exactly: kb/intents.pl, modules/intent_resolver.pl, t/test_todo_surface_toggle.py, zara/agent/tools/registry.py, zara/console.py, zara/python_skills.py.
- Comparing the stale base to current master shows none of those six paths changed upstream, so replaying the exact bounded implementation does not overwrite intervening Zara work.
- Android PR #269 is the active sibling transaction and is excluded from this recovery.

** Design / adversarial check
- Preserve one built-in todo authority when enabled and remove its complete public surface when disabled: Prolog intent routing, Python skills, and LangChain built-ins.
- Keep timer/alarm routing independent and available.
- Reject non-boolean todo.enabled instead of coercing strings or integers.
- Allow an external provider to claim otherwise-colliding todo tool names only after the built-in surface is disabled.
- Do not add a second todo store, timer store, command router, or plugin implementation.

** TDD evidence
- Existing PR #248 carries deterministic regression coverage in t/test_todo_surface_toggle.py for enabled/disabled behavior, Prolog routing, timer preservation, invalid config, and replacement-tool registration.
- Recovery does not treat stale CI as a merge gate. A fresh exact-head full repository/Nix CI run is mandatory after replay.

** Implementation plan
1. Replay the exact six-file #248 implementation onto the immutable recovery start.
2. Verify refreshed diff contains only those six paths plus this RAGE log.
3. Open replacement PR, close stale PR only after replacement exists, and require fresh exact-head CI.
4. Merge only if exact head is green, mergeable, non-draft, and discussion-free.

* Iteration 2 — full-suite PySWIP crash
:PROPERTIES:
:red_head: a50bf75c250a30c6748091e1bf845e41bea75bd9
:red_run: 33495910194
:END:

** RED
- Exact-head CI passed whitespace, Arch shared-mic, Ubuntu shared-mic, and Android skeleton gates.
- scripts/test-all.sh reached phase 9 and later segfaulted while t/test_wake_words.py constructed another PrologEngine and PySWIP consulted a fixture.
- The same process had already emitted a faulthandler timeout in the packaged-server shutdown test and retained a pycares shutdown thread.
- Flake/package stages were skipped; the failed head was not treated as mergeable evidence.

** Analysis / adversarial check
- PrologEngine documents PySWIP as one process-wide SWI-Prolog runtime shared by all engine instances.
- The new #246 regression test added an unnecessary in-process PrologEngine/main.pl consult before later existing Prolog tests.
- Changing production lifecycle code would expand the #246 slice without proof. The smallest safe correction is to isolate this semantic assertion in its own swipl process.

** GREEN candidate
- Replace the #246 Prolog regression's in-process PySWIP engine with a bounded `swipl -q -s main.pl -g ... -t halt` subprocess.
- This still proves default todo routing, disable semantics, search-todo removal, and timer preservation while leaving the shared PySWIP runtime untouched.
- Fresh exact-head CI remains mandatory; the candidate is not green until all repository-native gates pass.
