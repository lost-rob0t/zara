#+title: RAGE Gate Evidence - Zara Issue #129
#+startup: overview

* Continuation

- Parent work log :: =rage/daemon-139.org=, Iteration 2.
- Consumed issue :: #129.
- Immutable RAGE start commit :: =b0bc03590215424db01277cbc4758067a7b854ab=.
- Branch :: =rage/129-zara1-protocol=.
- Rule :: append-only continuation for Slice B onward and final gate evidence.

The original TASK #139 work log contains the immutable run identity, queue proof, research/design artifacts, and Slice A/B red evidence. This continuation exists so later exact-head evidence can be appended without rewriting earlier history.

* Exact-head regression after transport/server integration

- Red candidate :: =eff3fda6ff91e7e7e4cc59b04cdd6f58996ddec8=.
- GitHub Actions :: CI #270, run =32570146324=.
- Focused ZARA/1 phase :: 76 passed.
- Full pytest :: 5 failed, 828 passed, 14 warnings.
- Arch shared-mic :: PASS.
- Ubuntu shared-mic :: PASS.
- Nix flake/build :: skipped after the expected pytest failure.

All five failures were one coherent Unix-domain-socket boundary defect:

1. default =zara-server.sock= under a long CI runtime directory exceeded libzmq's =IPC_PATH_MAX_LEN=;
2. deterministic fallback for long paths was missing;
3. filesystem-byte length, including Unicode paths, was not enforced;
4. explicit overlong =ipc://= endpoints were accepted until bind.

Classification :: ordinary implementation defect exposed by committed TDD coverage. The ROUTER/DEALER, reconnect/idempotency, route fairness/backpressure, protocol, and distro audio tests remained green; no design invariant was falsified.

* IPC endpoint production fix

- Production commit :: =67b0e8dd2ec1c076a1865e583d876e9171bb276f=.
- Behavior :: short runtime paths keep =<runtime>/zara-server.sock=; overlong defaults use a deterministic hashed socket name inside =/tmp/zarathushtra-<uid>/=; fallback directory is current-user owned and mode 0700; limits are measured using filesystem bytes; explicit overlong IPC endpoints fail before bind.
- CI #271 :: started as production-fix proof; superseded as final authority by the later focused-gate expansion.

* Focused gate coverage correction

Static acceptance review found =scripts/test-zara1-protocol.sh= omitted three #129-owned suites even though the full pytest phase eventually exercised them. The focused gate was expanded instead of accepting that blind spot.

- Gate commit :: =0fdfaebbc4454c6cdecdf8683fc242b38775242e=.
- Focused gate now includes protocol framing, runtime codecs, core transport, reconnect/idempotency, route backpressure/fairness, transport status, and server IPC endpoint/lifecycle integration tests.
- Exact-head CI #272 :: pending at time of this checkpoint.

* Turn-completion client regression

- Red candidate :: =61ef9d8e4bcce125ad978d1bc573cc711330e158=.
- GitHub Actions :: CI #282, run =32571790109=.
- Focused ZARA/1 phase :: 73 passed before the first failure.
- Failure :: =t/test_zmq_transport_turn_completed.py::test_client_publishes_turn_completed_as_agent_completed_event= expected =events.AgentCompleted= for an unsolicited =turn.completed= wire event, but the client event subscription stayed empty.
- Arch shared-mic :: PASS.
- Ubuntu shared-mic :: PASS.
- Nix flake/build :: skipped after the focused regression failure.

Classification :: ordinary implementation defect, not a design failure. The server runtime codec already emitted =turn.completed=; =ZmqZaraClient._publish_runtime_event= simply lacked the inverse mapping.

* Turn-completion production fix

- Production commit :: =52b447cc9eb7bcf050bba279f03af1b123b9bc3b=.
- Behavior :: map =turn.completed= to =events.AgentCompleted= while preserving canonical turn/conversation IDs and the explicit success flag.
- Diff scope :: two production lines in =zara/zmq_transport.py=.
- Exact-head CI #283 :: superseded by the final documentation-bearing candidate below.

* Final gate

- Exact candidate :: =3ab7d04c6af2e62f00570fb9ebdc731c4eb075c0=.
- Pull request :: #142.
- GitHub Actions :: CI #284, run =32572470202=.
- =scripts/test-all.sh= :: PASS, including the complete focused ZARA/1 protocol/transport gate and full repository regression/security/packaging phases.
- =nix flake check= :: PASS.
- packaged =nix build= :: PASS.
- Arch Linux shared-mic :: PASS.
- Ubuntu 24.04 shared-mic :: PASS.
- PR mergeability :: mergeable, no unresolved review threads.
- Acceptance audit :: clean for #129. Authentication, hard principal isolation, voice streaming, and client-surface migration remain in their owning later issues.

The exact candidate above was promoted from draft and merged using expected-head SHA protection. No post-gate feature change was included in the merge.

* Outcome

- Merge SHA :: =1cae2e32cc40112aa13af6b8f4b7319869c72bd9=.
- Issue #129 :: closed completed.
- Epic #127 :: updated with #129 completion and next dependency-satisfied slice.
- Roadmap #1 :: updated with #128/#129 completion.
- Next ordered daemon issue :: #130.
- Queue rule :: #130 implementation is deliberately deferred to the next RAGE iteration; this iteration ends at the #129 merge and audit record.
