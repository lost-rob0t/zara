#+title: RAGE #209 - canonical resumable runtime tool approval lifecycle

* Immutable start
- Consumed issue :: #209, P0 blocker - implement canonical resumable runtime tool approval lifecycle.
- Parent epic :: #127.
- Blocks :: #191 and #134.
- Declared dependencies :: #131 and #133; both were closed before consumption.
- Exact RAGE start commit :: =2c608e7d4d79dc76ebfcc16bf8f064174a38df3b=.
- Branch :: =rage/209-runtime-tool-approvals=.
- Start worktree audit :: only the pre-existing user-owned unstaged =LICENSE= deletion was present; it is outside scope and must not be staged or modified.
- Protocol :: repository Auto-RAGE with Analyze, Design, Adversarial Review, autonomous Decision, tests-first Realization, complete local gates, exact-head GitHub Actions, and merge only when exact-head green and mergeable.
- Scope boundary :: implement one transport-neutral approval authority in the existing production runtime/LangGraph tool path. Do not add ZARA/1 messages, a second executor, arbitrary tool/shell invocation, the future #150 device capability plane or Prolog-RLM.

* Iteration 1 - 2026-08-24
** Analyze / Research
- Completed in =rage/209-runtime-tool-approvals-research.org=.
- The installed LangGraph API was exercised in the Nix shell: checkpointed =ainvoke= returned =__interrupt__= and resumed with =Command(resume=...)= under a stable thread id.
- Research rejected a plain Future-only gate, a second executor, a broad migration to middleware, and unsafe cross-process resume of generic non-idempotent tools.
- Selected restart policy :: live asynchronous approval is resumable; timeout, cancellation, restart and shutdown fail closed and erase the process-local checkpoint.

** Architecture / Design
- Completed in =rage/209-runtime-tool-approvals-design.org=.
- One principal-owned controller coordinates RuntimeHost decisions with a pure LangGraph interrupt node; a separate gated node delegates approved calls to the existing =ToolNode=.
- Server configuration is approval-policy authority. Approval envelopes and events never include tool arguments, results or user-provided rejection text.

** Adversarial review
- Completed. The design now gathers every approval before any side effect, atomically consumes pending ids, preserves original tool-result ordering, revalidates tool bindings, and defines the exact cancellation/start boundary.

** Autonomous decision gate
- ACCEPT. Tests-first realization is authorized. Cross-process pending approval survival remains deliberately rejected until the typed capability plane provides durable idempotency and reconciliation.

** Realize / TDD
- Authorized. Next action is the focused failing production-backend approval lifecycle test.

** Evaluate
- Pending realization.

** Outcome
- In progress.
