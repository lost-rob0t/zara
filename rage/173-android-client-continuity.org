#+title: RAGE #173 — Android ZARA/1 client continuity and text transport
#+property: issue 173
#+property: start_sha ceb2e13951af518cbc29df2dafabdea022baad89

* Consumption
- Issue :: #173 — authenticated Android ZARA/1 enrollment, reconnect and session continuity.
- Exact start :: =ceb2e13951af518cbc29df2dafabdea022baad89=, canonical master after #322 Android auth merged.
- Owner :: Zara Android Auto-RAGE worker.
- Scope :: Android-only endpoint/settings model, canonical connection reducer, bounded reconnect/session generations, ZARA/1 client framing/correlation and real typed text-turn client path. Shared compatibility is limited to the already-approved #159 contract.
- Core dependency :: #329 / PR #331 owns production secure TCP listener. Do not modify Core files here.

* Live-state correction
- #322 merged the credential wrapping/server-pin substrate and auto-closed #173, but #322 explicitly did not implement connect/hello/reconnect/text-turn acceptance.
- #173 was reopened on 2026-09-05 so issue state again matches its acceptance contract.
- #172 and #133 are satisfied.
- #159 successor #336 passed exact-head CI #1104 and merged to canonical master as =eb6e9bac0a9fc13ca90b3779fe1d7271107d431e=.
- Recovery helper #341 merged that canonical master into this branch as =7ad60ae16aea72ba614e3b2b80334ab904efd742=, so Android #173 now consumes the real typed capability/action contract rather than a stale assumption.
- Production stock-daemon secure TCP remains Core #331 and is the only external end-to-end blocker for a real device-to-stock-daemon authenticated smoke.

* Design boundary
- Android remains a client of the one Zara daemon/runtime; no local LLM, server, conversation database or memory authority is added.
- Enrollment material from #322 remains the sole CURVE identity/pin authority.
- One Android runtime reducer owns connection/session state rendered by future Compose surfaces.
- A successful TCP/CURVE socket alone is not =Connected=: only canonical =hello.ok= for the current generation may establish an application session.
- Reconnect creates a new transport/application generation. Old-session events/replies are rejected rather than applied to current state.
- Durable conversation identity and public server profile are retained separately from ephemeral ZARA/1 session identity; no invented =session.resume= message.
- Endpoint configuration is public metadata and must never contain embedded user-info credentials or client private material.

* First implementation map
1. TDD endpoint validation + server profile value.
2. TDD reducer states for configured/enrollment/auth/session/error/reconnect with generation and bounded retry.
3. TDD deterministic backoff calculation without wall-clock sleeps.
4. TDD closed ZARA/1 frame codec for =hello=, =hello.ok=, =turn.submit= and text/runtime events.
5. JeroMQ DEALER owner using the #322 CURVE configurator and typed generation/correlation tables.
6. Deterministic controller orchestration for real bounded reconnect and stale completion rejection.
7. Consume merged #159 with a closed capability-snapshot codec; advertise no platform capabilities until #174 provides real adapters.
8. Fake/inproc deterministic client tests now; stock secure TCP interoperability after Core #331.
9. Android gate + full repository/Nix gate + exact-head Actions.

* Invariants / adversarial cases
- wrong/missing pin, missing/corrupt credential and socket auth failure never downgrade to plaintext;
- malformed/oversized/wrong-magic/unknown message input fails closed;
- duplicate reply cannot complete two requests;
- stale generation/session response/event cannot mutate current conversation/runtime state;
- reconnect attempts and pending requests are bounded;
- no transcript, private key, token or raw credential enters diagnostics;
- process recreation may restore public server profile + durable conversation id, never fabricate a live session;
- capability advertisement is availability only and cannot create server authority.

* Iteration 1 — dependency-safe client state
** TDD / implementation
- Server-profile RED :: =f17105cb1daaa33d6c9757aa9d636602696979ac=.
- Server-profile GREEN :: =9abadedaac91557e344135c233a593f8898d31fa=. Only canonical =tcp://host:port= endpoints are accepted; user-info credentials, paths, queries/fragments, missing host/port and non-TCP schemes fail closed.
- Connection-reducer RED :: =9d13265325f85ada47cb7852063f782121dbfda1=.
- Connection-reducer GREEN :: =0c9a971dfead6170af221ea94b35539268e3cc06=. =hello.ok= is generation-bound; reconnect increments generation, clears ephemeral session, rejects stale generation input, retains selected durable conversation id and caps exponential retry at 4 seconds / configured attempt limit.
- Existing launch renderer was updated at =9edcc1c97a504f19752484d19de9f485b71112de= to compile against the one evolved reducer rather than introduce parallel UI state.
- Restore-state RED :: =696d832cdcafe9cd2bca9a95083cee97128369ca=.
- Restore-state GREEN :: =81adb076b2d32650f0b8430a2a0722fbebefbb17= plus =69c76809abc8d6f128e012fcde8d8d1ee7c3b2f9=. The bounded atomic store persists only public server profile + durable conversation selection. Corrupt/oversized state fails closed. Rehydration always starts disconnected with no session id.
- Request-correlation RED :: =605993c0f533bc7907588b2b8b90c04d61253c7f=.
- Request-correlation GREEN :: =ef7c7b832962679efdf0296fd802cba16575f9ee=. Pending request ids are bounded and bound to generation + session; stale replies cannot consume current requests and duplicate completion becomes unknown.
- Enrollment projection RED :: =a57c02a7ca5b072fc5fa87c40df90ebf3ec75715=.
- Enrollment projection GREEN :: =b5c54cfb8242488ffac0ab459efab80e10383c1c= + =1a275917e20f0548b630c55a14f228b5cd74f70b=. UI/runtime state receives only a key-free readiness projection from the authoritative #322 =EnrollmentRepository=.
- Launch wiring :: =d354638d035ddddeaaa5bd967046005ffda2acde= restores public client state and reads real enrollment authority on process launch; it does not fabricate connection/session continuity.

** ZARA/1 text path
- Codec/tests cover closed =ZARA/1= hello, turn submit/accept, assistant response/delta/completed and protocol-error framing with bounds and unknown/malformed rejection.
- =JeroMqTextDealerFactory= is the production DEALER edge. It consumes the one #322 =EnrollmentRepository=/CURVE configurator, applies authentication before connect, uses bounded receive timeouts and closes socket/context ownership together.
- =ZaraTextClientActor= owns the blocking DEALER on one dedicated executor thread rather than Activity/UI threads. It performs canonical hello negotiation, records generation+session ownership, submits typed text turns, requires request/reply correlation, validates turn/conversation/session identity on streamed events, and rejects stale sessions.
- Actor tests use deterministic fake dealers/request ids/timestamps to prove hello, real typed text-turn result, timeout/error, stale-session and ordering behavior without a second assistant runtime.
- Controller RED existed as =AndroidTextSessionControllerTest= before production controller implementation.
- Controller GREEN :: =4353214b9a3e96a0e2d70986722117d09d5f71a7= creates the one reducer-owned orchestration seam; =759222b879825cf34bff226f247ef1a93eccae0e= binds the existing actor to that =TextSessionClient= seam.

** Exact source diagnostics
- Initial BuildConfig source field :: =985e56d365c92eeaccf79550cbf8ba36ccc39d32= and launch display =f78f54c92a1a293824441a7fa32dadad4dcb3002=.
- Initial PR-checkout attempt used =HEAD^2= when =GITHUB_HEAD_REF= existed. CI #1107 showed the Android job uses a shallow pull-request checkout, so that parent is not guaranteed to exist even though the source branch is valid.
- Build gate repair :: =c2a1547f88d59af29da58ffd6dbbc6ac30077f7c=. Gradle now reads immutable =pull_request.head.sha= from GitHub's local event payload when present, falls back to =git rev-parse HEAD= outside PR events, preserves explicit =ZARA_SOURCE_SHA= override, and validates the result as a lowercase 40-character SHA before generating BuildConfig.
- Regression =BuildMetadataTest= still requires the diagnostic source identifier to be an immutable 40-character git SHA; no assertion was weakened.

* Iteration 2 — actual reconnect + canonical #159 consumption
** Restored profile continuity
- RED :: =bb55b494bbbdb8c93e73b5facb0646e6102b9d9e= proves process recreation must restore the public server profile while remaining disconnected/sessionless.
- GREEN :: =a4173f79ec5bb51ea54c1711663ae2872afa0228= makes =configuredProfile= part of canonical =RuntimeState= and restores it from the bounded public =ClientStateStore= without fabricating generation/session state.

** Bounded reconnect execution
- RED :: =6f6dabd20578abad4218b4173883ed141dc44e7b= requires a lost connection to schedule a deterministic 250 ms generation-2 reconnect, commit only that fresh hello, then advance failure retries to 500/1000/2000/4000 ms under the existing reducer limit.
- GREEN :: =c28f43701b5b7196dc64f2050966a2cea0116af2= adds one =ReconnectScheduler= owned by the same controller. Production uses one daemon scheduled executor; tests use a deterministic queue with no sleeps. Each retry reuses =configuredProfile=, clears the old dealer, remains generation-bound and stops once closed/degraded.
- Stale text completions still cannot mutate selected durable conversation state after generation rollover.

** #159 capability negotiation substrate
- #159/#336 exact-head CI #1104 passed and canonical merge =eb6e9bac...= is now in this branch via helper #341.
- Capability codec RED :: =acefb7b80abe4b7c59a492b5033245aa8a0c7587= requires closed/sorted/session-bound v1 snapshots, exact acknowledgement parsing, duplicate rejection, closed ids and version =1=.
- The codec was intentionally isolated from the text-turn codec so #174 platform adapters can consume it without widening text parsing or creating a parallel runtime.
- GREEN implementation :: =04ef8a261570f5a45639314b74fae404c9f354ce= plus parser hardening =af5a27979d66cbea75062f829dc565e17955d72e=. Only =open_app= and =open_uri= exist; unknown/admin ids, wrong versions, extra fields, duplicates, malformed JSON/UTF-8 and nonzero payload assumptions fail closed.
- Current first-text milestone exposes no device adapter success until #174 wires actual Android actions. Capability visibility remains availability, never authority.

* Gate
- PR :: #338 remains draft while stock-daemon integration is blocked by Core #331.
- Historical CI #1107 at =d8787394a62fcca108bebc7c364937b011a972b1= had repository tests and mic lanes green but Android gate RED because shallow PR checkout invalidated =HEAD^2= source-SHA derivation; that defect is repaired, not bypassed.
- Current exact candidate after this log is the branch head containing =af5a279...= plus this evidence commit. Fresh exact-head Android unit/native/debug-APK/secret inspection and full repository/Nix/package Actions are required. No prior SHA authorizes merge.
- Core #329/#331 remains the only blocker to deterministic stock =zara-server= authenticated TCP hello/text-turn interoperability. It is Core-owned and untouched here.

* Next
- Freeze this exact head until CI surfaces the real Android verdict; consume any deterministic RED rather than superseding it.
- Once the candidate Android gate is green, preserve its debug APK artifact and source SHA.
- After Core #331 lands, recover current master, add a real authenticated stock-daemon hello/text-turn interoperability gate, then close/merge #173 only if exact-head green.
- Immediately proceed to #197 Compose Chat + Connection/Enrollment + Settings + Diagnostics for the first-usable APK; do not wait for Samsung/assistant-role work.

* Iteration 3 — canonical terminal turn lifecycle, active capability negotiation and stale-retry hardening
** CI RED — hidden text lifecycle mismatch
- Exact candidate =5c97b44ce7f52490e048a867416a12bc9e1f4457= ran as CI #1140.
- Shared-mic lanes were green and semantic/Trealla parity completed, but the Android gate failed compilation in =ZaraTextClientActor=: the sealed =TextServerMessage= =when= did not handle =Progress= and =TurnCompleted=.
- The debug-APK upload step executed after failure but no APK existed; CI #1140 therefore produced parity diagnostics only, not a valid exact-head APK artifact.
- Existing =ZaraTextTurnOrderingTest= already encoded the stronger canonical contract: the actor must consume =turn.started= / =assistant.started= / =assistant.completed= through terminal =turn.completed=. The production actor instead returned at =assistant.completed=, so adding a catch-all =else= would have hidden a real lifecycle bug.

** Terminal lifecycle GREEN
- Production repair :: =9a8d7df580e8ef155cd961f01e1fd8ee74986320=.
- =Progress= events are now explicitly session/turn/conversation verified.
- =assistant.completed= records one bounded candidate result but is not terminal; duplicate assistant completion fails closed.
- Only matching terminal =turn.completed= may complete the streamed typed turn, and its =success= must agree with the assistant completion.
- Stale/mismatched terminal events remain rejected. The legacy typed =assistant.response= path remains explicit rather than becoming a generic catch-all.
- Fixture repair :: =d591e395878962cc10517d8bde71eedc031cd27b= adds the canonical =turn.completed= frame to the older actor happy-path fixture. This does not weaken the oracle; it aligns that fixture with the pre-existing ordering test.

** Active #159 negotiation RED/GREEN
- RED :: =d74b978f14124ee516d58250dde49d6d0f60d455= requires a successful hello to be followed by an empty, session-bound =capability.snapshot= and exact =capability.snapshot.ok= before the actor reports Connected.
- GREEN :: =0e552e13e2ccb351c19af3ac7273e895b50f5353= performs that negotiation on the actor's one authenticated DEALER.
- The acknowledgement must correlate to the capability request id and current hello session. Any server acknowledgement of a capability Android did not advertise fails closed.
- The intentionally empty snapshot is the honest #173 state: it exercises merged #159 on every connect/reconnect while advertising no Android device-action success until #174 supplies real platform adapters.
- Actor/reconnect fixtures were upgraded at =fb53318443f56de52cc3533729876a90518f1647= and ordering coverage at =9330314a8a201f6adc3b61d266c90ef1f36d7904= so both first connect and reconnect negotiate the closed snapshot before text traffic.

** Stale reconnect completion race RED/GREEN
- Adversarial review found a reducer/side-effect split race: a stale generation's failed connect was correctly rejected by =reduce=, but controller code then checked only whether the *current* state was Reconnecting and could schedule a duplicate retry/disconnect against the newer generation.
- RED :: =0af3860c5784572b4697d1c9a990e59d3dfef797= deterministically advances from generation 2 to generation 3 while generation 2 remains in-flight, then completes the stale generation 2 future exceptionally. The required oracle is no additional delay and no extra disconnect.
- GREEN :: =81e7aafc6fc4b36c1c3aec5535a5fdada6b39ca9= rejects stale connect completions before any reducer mutation or retry/disconnect side effect. Current-generation failures continue through the existing bounded retry path.

** Core handoff state
- Core-owned #329 / PR #331 exact head =fabea77133914976bd0da2373ad9392d42a46a1e= has all four exact-head CI #1138 jobs green, including the secure TCP integration and Android build lane.
- #331 remains draft and Core-owned. Android does not mark it ready, merge it, or modify its Python/security files.
- Once Core lands it, #173 must consume current master and prove the Android authenticated stock-daemon hello + text turn rather than relying on a custom fixture.

** Current exact gate
- Behavior candidate before this evidence append :: =81e7aafc6fc4b36c1c3aec5535a5fdada6b39ca9=, CI #1148.
- At evidence-write time both shared-mic lanes were green; Android unit/native/debug-APK/secret gate and full repository/Nix/package gate were still running. No green or APK claim is made for that head yet.
- This append creates a new exact head, so the post-evidence Actions run is authoritative for any later merge/readiness claim.