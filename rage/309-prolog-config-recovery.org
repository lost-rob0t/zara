#+title: RAGE #309 — Prolog config recovery

* Transaction
- Consumed issue :: #309 — P1 regression — finish Prolog config recovery: Brave default and remove dead RLM knobs
- Immutable start :: e6d9409e49246a187d5ea015da681bfc4e4fdc63
- Branch :: rage/309-prolog-config-recovery
- Scope :: Zara Core configuration/tests/docs only. No Android production, external Prolog-RLM, dotfiles/Home Manager, plugins, or other repositories.

* Iteration 1
** Research
- Evidence :: =rage/309-prolog-config-recovery-research.org=
- #5's =config.pl= + =config.local.pl= ownership split is correct and stays authoritative.
- Current master still defaults search to DuckDuckGo and still exposes/configures dead =prolog_rlm_*= predicates despite roadmap/RAGE policy prohibiting that runtime path.
- =wiki/install.org= additionally documents an RLM module/live-smoke path that is no longer a supported Zara configuration path.
- Brave's current public search surface is =search.brave.com=; this slice keeps search configuration as a data URL template rather than adding a provider/runtime layer.

** Design
- Evidence :: =rage/309-prolog-config-recovery-design.org=
- Change only the built-in search template, supported Prolog config schema/examples, and stale config documentation.
- Preserve local-overlay precedence, server/device fact validation, normal SWI-Prolog, and normal LLM provider settings.
- Old =prolog_rlm_*= facts must fail closed through the existing unsupported-fact domain error.

** TDD
- Test-only recovery unit :: =t/prolog_config_recovery.pl=
- Gate inclusion :: =scripts/test-prolog-config.sh=
- Initial test-only head :: =28f4c7d7658cd8780a447f20612d7382fe4920ed=.
- CI #882 reached the full repository =Run all tests= step and reported success instead of the expected failure. This is not accepted as RED evidence; it exposed faulty test-unit invocation in the focused shell gate.
- First harness correction attempted separate PlUnit summaries, but exact-head CI #884 again false-greened: =Run all tests= and =nix flake check= both passed while no production/config/docs implementation existed. The summary-based shell expression was therefore not accepted as a fail-closed test gate.
- Second harness correction invoked each test unit in its own SWI-Prolog process, but exact-head CI #886 still false-greened at =504b38246f19fcc7eedd506786a1f92e6865d68c= while =kb/config.pl= still contained the DuckDuckGo default and dead RLM predicates. This result is also rejected as RED evidence.
- Third harness correction added a direct semantic precondition to =scripts/test-prolog-config.sh= that requires the Brave default before the recovery PlUnit suite runs.
- Repository gate hardening fixed =scripts/test-all.sh= so a failing command inside a phase can no longer be overwritten by a later successful command under Bash =errexit= suppression.
- Genuine RED head :: =329e3db39ff13516b8aceb86e869de363be79229=.
- Exact-head CI #889 :: failure in =Run all tests=; Arch shared-mic, Ubuntu shared-mic, and Android gates all passed. =nix flake check= and packaged build were correctly skipped after the failed full-test phase.
- Pytest artifact :: 1675 tests, 0 failures. This isolates the RED to the intended shell/Prolog-config gate rather than a Python or Android regression.
- RED accepted. Production implementation may proceed.

** Implementation
- First production commit :: =b6220dea467660ed6cdf9aded0eaeeb1222abf09=.
- =kb/config.pl= now defaults =search_engine/1= to Brave Search, removes exported/dynamic/default =prolog_rlm_*= predicates, and points mutable/private operator overrides at =config.local.pl= while retaining provisioned =config.pl= compatibility.
- Exact-head CI #891 at =0412cae73595f75a85e54b9ef058daca8dc34022= remained RED in =Run all tests= while pytest, Android, and both shared-mic jobs were clean. This was expected remaining contract work, not a new runtime regression.
- =modules/config_loader.pl= now rejects =prolog_rlm_enabled/1= and =prolog_rlm_model/1= in both desktop and server config scopes, removes generated RLM instructions, explicitly teaches the provisioned =config.pl= versus mutable/private =config.local.pl= ownership split, and uses Brave for its no-loaded-config search fallback.
- The legacy =t/prolog_config.pl= contract was reconciled with the new supported schema: RLM facts are no longer treated as valid configuration and default-search expectations now use Brave.
- Adversarial adjacent-runtime review found =modules/command_loop.pl= still dereferenced the removed =kb_config:prolog_rlm_enabled/1= predicate. That would turn the config cleanup into an undefined-predicate failure on the fallback path. The dead conditional/import was removed; ordinary bounded LLM rewrite remains the fallback.
- =wiki/install.org= and =wiki/api-service.org= now document the durable XDG ownership split and no longer advertise the removed RLM configuration path.
- No =android/**= production files or external repositories were touched.

** Evaluate
- Candidate implementation head before final evidence commit :: =8d49d942e6fc54fa94a333ebd2591496ed541697=.
- Exact-head full repository/Nix/package CI is required before promotion or merge; any pending or failing head remains non-authoritative.
